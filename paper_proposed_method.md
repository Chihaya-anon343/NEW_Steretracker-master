# 3. 提出的方法

## 3.1 系统总体框架

本文提出的多模态目标位姿跟踪系统以双目相机（或单目相机）作为感知输入，面向无人机视觉定位场景，在统一的处理框架内实现了从目标检测、状态判定、特征提取到位姿估计的完整闭环。系统的整体流水线如图[图3-1]所示。在每一帧处理周期内，系统首先对左右图像对分别执行基于轻量级目标检测网络的检测与兴趣区域（Region of Interest, ROI）生成，随后依据目标的投影面积将当前观测划分为五个相互衔接的目标状态，并据此从预置的多组特征提取策略中选择最适宜的主策略及其后备策略；特征提取完成后，系统将二维观测点与模板三维点之间的对应关系送入位姿解算模块，通过最小化重投影误差或立体交叉射线误差得到目标的六自由度位姿，并经后校验环节确认输出结果的物理合理性。

在整体架构上，系统遵循"检测—分级—提取—解算"的模块化设计原则，各模块之间通过统一的数据载体解耦，从而使得不同特征策略之间可以灵活组合与替换，也为后续引入多模态信息融合预留了接口。需要说明的是，目标检测环节采用公开的YOLO系列网络，输入与输出子系统仅承担图像采集、帧同步、可视化渲染与日志记录等基础功能，这些组成模块的技术本身并非本文关注的重点，在此不再赘述。本文的核心贡献集中于四个方面：（1）面向连续距离变化的目标五状态分级机制；（2）与各状态相匹配的混合特征提取策略库及其内在的互补性设计；（3）单向退化后备链机制，为特征提取失败提供了系统级的鲁棒性保障；（4）融合立体交叉射线约束的位姿解算方法，实现了跨状态的高精度位姿估计。

[图3-1: 系统整体流水线示意图（目标检测→五状态分级→策略选择→特征提取→位姿解算）]

## 3.2 基于面积判定的五状态目标分级

无人机在接近目标的过程中，目标在成像平面上的投影面积会发生跨越数个数量级的变化。当目标距离较远时，其在图像中仅占据数十像素见方的小区域，轮廓退化、纹理信息几乎完全缺失，此时以局部特征点检测为基础的方法（如基于描述子的匹配方法）将因关键点数量过少而失效；随着无人机逐渐靠近，目标轮廓变得清晰，边缘与角点结构成为最稳定的观测线索；而当目标距离进一步拉近时，目标的表面纹理又逐渐丰富起来，为高判别力的描述子匹配提供了条件。这一观察表明，**不存在任何一种单一的特征提取方法能够在目标的整个工作距离范围内保持稳定可靠**——不同方法各自的适用区间在距离维度上是互补的。基于这一认识，本文提出了基于投影面积判定的五状态目标分级机制，通过将连续的观测过程离散化为若干具有明确语义的状态，为每一种状态匹配其最适宜的特征提取策略。

目标检测网络的输出包含两类目标：class 0 对应于目标的整体轮廓，用于界定目标在图像中的全部范围；class 1 对应于目标的中心区域，用于在目标整体被遮挡或部分移出视野时提供备选的观测锚点。系统的状态判定以 class 0 的边界框面积为主要依据，其判定逻辑为：当面积不超过远距离阈值时判定为远距离状态（State 1）；当面积处于中等区间时判定为中等距离状态（State 2）；当面积处于中等偏大区间时判定为中近距离状态（State 3）；当面积超过近距离阈值且 class 1 同时存在时判定为近距离状态（State 4）；而当 class 0 丢失、仅剩 class 1 且其面积达到设定下限时，判定为极近距离状态（State 5），此时系统以 class 1 的边界框重新生成 ROI 并再次按面积进行重分类。状态判定的形式化条件如[公式3-1]所示。

[公式3-1: 五状态分级判定规则，s为class 0边界框面积，T为各级面积阈值]

值得强调的是，上述面积阈值并非依据相机内参与目标物理尺寸推导所得的解析值，而是针对具体部署场景预先标定的经验参数，其在物理上等价于将目标的深度范围映射为投影面积区间。本文以面积而非深度作为状态判别的依据，原因在于面积可以直接从检测结果中获得，不依赖任何位姿先验，从而避免了分级环节引入误差。五种状态、其判据、对应的主提取策略与位姿求解途径的对应关系汇总于[表3-1]。

[表3-1: 五状态总览表（状态、面积区间、主特征策略、位姿求解方式、warm-start与退化途径）]

## 3.3 多策略混合特征提取

针对五种目标状态在图像外观上的显著差异，本文构建了一个包含三种基础特征提取器与一种混合提取模式的策略库，分别为面向中近距离纹理丰富目标的 AKAZE 描述子匹配策略、面向中等距离多边形标识物的二值角点提取策略、面向远距离微小目标的超分辨率微目标提取策略，以及面向近距离大目标的 Dual-ROI 混合提取策略。三种基础策略在特征模态上相互独立——描述子特征、形状角点特征与像素强度分布特征分别利用了目标在纹理、轮廓与整体外观三个不同层面的信息，这种模态层面的互补性使策略库能够在退化链机制下逐级支撑（详见3.4节）。以下分别对各策略的详细设计进行阐述。

### 3.3.1 AKAZE 描述子匹配策略

AKAZE 描述子匹配策略面向中近距离、表面纹理丰富的目标，是系统在绝大多数正常跟踪情况下使用的主策略。该策略在特征提取阶段同时利用了目标的局部外观描述与双目图像的几何约束，其处理流程如下：首先在 ROI 内以一定的尺度因子对图像进行降采样，以提高 AKAZE 关键点检测的计算效率；随后在降采样图像上执行 AKAZE 检测与二值描述子计算，并将关键点坐标还原至原始分辨率。为建立左右图像之间的特征对应，该策略没有直接采用描述子匹配，而是引入了金字塔光流追踪，分别执行左图到右图的正向追踪与右图到左图的反向追踪，并以双向一致性的前向后向误差阈值剔除追踪失败或遮挡引起的错误对应。在获得可靠的立体匹配点对之后，系统依据相机内参与基线将视差转换为深度，反投影得到三维点，并重投影到右相机以检验投影一致性。与此同时，为建立图像特征与模板特征之间的对应，系统对左图关键点与模板关键点执行包含比率检验、交叉检验与单应性随机抽样一致性检验三个阶段在内的级联匹配，其中比率检验以距离比值准则剔除模糊匹配，交叉检验利用双向最近邻的一致性过滤单向误匹配，单应性变换估计进一步以平面假设约束剔除几何不一致的离群点，从而在保证匹配精度的同时兼顾了运算效率。双目模式下，上述立体匹配与模板匹配的结果共同构成位姿求解的输入，而在单目模式下则仅保留模板匹配路径（单目差异在3.5节中统一说明）。

### 3.3.2 二值角点提取策略

二值角点提取策略面向中等距离、具备清晰多边形轮廓的目标。在此距离范围内，目标表面纹理尚不足以支撑描述子匹配，但其边缘与角点结构在二值化后具有极高的对比度，能够被稳定地提取与定位。该策略首先以自适应阈值方法（Otsu）对 ROI 进行二值化，将目标与背景分离；随后通过连通域分析选取面积最大的连通区域作为候选目标，并经过孔洞填充与形态学开闭运算平滑其边界。为消除目标平面内旋转带来的影响，策略预置了按 15° 步长旋转的 24 个方向的模板二值图，通过交并比度量在当前二值图中检索最佳匹配角度，并据此将目标旋转回正；随后利用多边形近似算法（Douglas–Peucker 的 RANSAC 变体 approxPolyDP）对目标轮廓进行压缩，通过二分搜索多边形近似参数以提取指定数目的角点，并由基于几何关系的角点重排序算法建立提取角点与模板角点之间一一对应的索引关系。这种先旋转回正、再提取角点、最后几何对齐的处理顺序，使得角点的提取与匹配对目标的平面旋转具有天然不变性，而无需依赖昂贵的特征描述子。

### 3.3.3 超分辨率微小目标策略

超分辨率微小目标策略面向远距离、在图像中仅占据极小面积的目标，该策略的设计目标是解决"目标过小、几何与纹理信息同时匮乏"这一最为困难的情形。针对远距离目标在低分辨率下的信息瓶颈，策略首先利用 Otsu 二值化分离目标与背景，并通过与 24 方向模板的交并比匹配确定目标的角度；随后对目标区域实施基于三次插值的固定倍数超分辨率放大，并配合高斯平滑与形态学运算抑制放大过程引入的噪声，从而在放大后的尺度上重建目标的细节结构。在此之后，策略以多指标加权评分的方式对候选连通域进行甄别，评分综合考虑矩形度、面积占比合理性、距 ROI 中心距离以及长宽比四个维度，以加权求和的方式选出最符合目标先验形状的连通域；最终以最小外接矩形提取目标的四个角点，并经亚像素精化与象限对齐两个环节将角点坐标稳定地对应到目标上已知的三维点。值得指出的是，该策略是退化链的终点，其提取结果直接决定远距离状态下系统是否能够输出位姿，因此策略在连通域筛选与角点对齐环节设置了多层内部校验，以最大程度避免将错误对应引入位姿解算。

### 3.3.4 Dual-ROI 混合策略

当无人机逼近目标至近距离时，目标的整体轮廓（class 0）与中心区域（class 1）同时在图像中占据较大的面积，此时单一特征模态的信息已不足以充分利用目标的全部外观线索。为此，本文提出 Dual-ROI 混合策略，在同一帧内对两个 ROI 并行执行两种模态的互补特征提取：一方面，对 class 0 所界定的目标整体区域执行二值角点提取，捕获目标边缘与轮廓的强几何约束；另一方面，对 class 1 所界定的中心区域执行 AKAZE 描述子提取，捕获中心区域丰富的纹理信息。由于两路提取分别在各自的 ROI 局部坐标系下进行，混合策略在合并阶段需首先将中心区域提取点平移至整体区域的局部坐标系下，完成坐标系的统一；随后依据模板匹配所确定的角度关系，以几何重排序方式确保二值角点与模板角点的一一对应，最终将两路的二维观测点及其对应的三维模板点合并为统一的最小二乘输入。通过融合边缘角点与内部纹理两类互补信息，Dual-ROI 策略在近距离状态下显著增加了位姿求解的约束数量，从而提升了优化解的稳定性。

### 3.3.5 CloseRange 回退策略

当目标运动导致 class 0 的检测框消失、而中心区域 class 1 仍可被检测时，系统进入极近距离回退状态。该状态是目标整体观测缺失时的一种补救机制：系统以 class 1 的边界框为基础重新生成 ROI，并再次依据其面积落入的区间重新判定状态，从而转入 State 1 至 State 4 中相应的正常策略链。为使回退后的策略选择与仅含中心区域的观测相适应，系统为中心区域场景提供了独立的面积阈值参数，以替换通用阈值参与策略链配置，从而避免了因目标语义变化而导致的策略误选。

## 3.4 单向退化后备链机制

在实际飞行场景中，光照突变、运动模糊、局部遮挡以及目标姿态变化都可能使当前帧所选主策略的特征提取失败。为保证跟踪的连续性，本文引入单向退化后备链机制，其基本思想是：当主策略提取失败时，系统并非直接宣告当前帧失败，而是沿预设的退化方向逐级降级到信息利用率更低、但鲁棒性更强的后备策略，直至某一策略成功或退化链耗尽。具体而言，策略链按照"AKAZE 描述子策略 → 二值角点策略 → 超分辨率微目标策略"的顺序组织，其中每一级策略在特征模态上与前一级具有本质区别，从而保证了退化切换真正带来了信息利用方式的改变，而非仅仅是参数的调整。退化链的触发条件形式化为[公式3-2]。

[公式3-2: 退化触发条件，success为特征提取成功标志，n_kp为有效关键点数]

退化链的设计遵循"单向、不可逆、无环"三项原则：退化方向始终从高判别力策略指向低判别力策略，且一旦降级便不跨区间折返，从而避免了策略之间的往复震荡；由于后备策略随主策略一次性配置，退化过程的实现开销极小。需要特别指出的是，系统对两类失败情形进行了语义上的区分：其一是目标检测未给出任何 ROI，此时系统直接跳过当前帧而不进入退化链；其二是存在 ROI 但特征提取失败，此时才触发退化机制。这一区分避免了在目标短暂出视时浪费计算资源。此外，Dual-ROI 混合策略作为近距离状态下的独立路径，不参与退化链，其任一支路的失败均直接视为当前帧提取失败，从而保持了该状态内部两条提取路径的语义一致性。退化链的全景结构如图[图3-2]所示。

[图3-2: 退化后备链全景图（策略主链、Dual-ROI独立路径、内部子退化机制）]

## 3.5 位姿解算

### 3.5.1 双目交叉射线 GPnP 优化

在双目配置下，系统采用基于交叉射线约束的全局位姿优化方法（GPnP）进行位姿求解。与仅最小化单目重投影误差的传统 PnP 方法不同，GPnP 直接利用双目相机提供的立体几何约束：对于每一个匹配点对，其左图观测与右图观测分别定义了相机坐标系下的两条观测射线，而模型三维点经当前位姿变换后应同时位于这两条射线上。据此，GPnP 将位姿求解构造为最小化模型点对两条观测射线距离的非线性最小二乘问题，其残差向量由左右视图各自的向量叉积残差拼接构成，如[公式3-3]所示。

[公式3-3: GPnP交叉射线残差定义，r_L与r_R分别为左右视图叉积残差]

上述优化问题以四元数与平移向量构成的七维向量作为优化变量，采用 Levenberg–Marquardt 算法求解。为加速收敛并避免陷入局部极小，系统在双目模式下维护帧间位姿缓存，以上一帧成功解算的位姿作为当前帧优化的初始猜测（warm-start）；当缓存缺失或首帧尚无位姿时，则以初始位姿解算器提供的初值启动优化。与重投影误差相比，交叉射线约束充分利用了双目基线的几何优势，在观测噪声存在的情况下对深度方向的估计更为稳健，同时七维优化变量结合 LM 算法的增量阻尼策略，使得 GPnP 在关键点数量较少（每帧仅需数个有效点）时仍能收敛到稳定的解。

### 3.5.2 首帧初值解算与单目位姿解算

为向 GPnP 提供可靠的初始猜测，系统在双目模式的首帧或 warm-start 缓存失效时启用初始位姿解算器。该解算器首先以随机抽样一致性（RANSAC）方式对 PnP 问题进行鲁棒估计，在采样迭代中剔除二维观测中的离群点，随后在筛选出的内点上以迭代式非线性最小化进行精化，从而在存在观测噪声与少量错误对应的情形下获得稳定的位姿初值；若初值解算失败，系统退化为以视差中位数直接估算深度作为初值。在单目模式下，系统采用基于有效点数为四的 EPnP 算法配合 RANSAC 进行位姿估计，并以迭代式精化提升精度。针对微小目标在近距离下二维观测点高度聚集、深度估计病态化而导致迭代精化发散的问题，单目解算器在精化前保存 RANSAC 的估计结果，并在检测到精化结果超出合理深度范围时自动回退至 RANSAC 结果，从而避免了将发散位姿误判为有效输出。

### 3.5.3 位姿有效性校验

所有位姿解算器输出的结果在进入状态缓存与可视化之前，均需通过统一的物理有效性校验，以保证估计位姿在物理语义上的合理性。校验条件包括：相机必须位于目标前方、位姿的平移范数须处于与目标工作距离相符的合理区间、旋转矩阵与平移向量的所有分量须为有限值、以及参与求解的有效点数须达到设定下限。上述校验以轻量级的逐项判定的方式实现，构成了位姿输出的最后一道安全保障，其形式化条件如[公式3-4]所示。

[公式3-4: 位姿有效性校验条件（前方性、深度范围、有限性、点数下限）]

## 3.6 基于 ESKF 的多模态信息融合

（本节为预留小节）
